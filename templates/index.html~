{% extends 'base.html' %}

{% block title %}
Главная
{% endblock %}

{% block content %}

<header>
    Добро пожаловать в список объявлений
</header>


    {% for d in data %}
    <h2>{{d}}</h2>
    {% empty %}
    <h2>Данных нет</h2>
    {% endfor %}

        {% for rub in rubrics %}
        <p><a href="{{ rub.get_absolute_url }}">{{ rub.name }}</a></p>
        {% endfor %}
    </div>
    <hr>

        {% for b in bbs %}
        {% if forloop.first %}

            <h1 style="color: purple"><a href="{{ b.get_absolute_url }}">{{ b.title }}</a> — <span class="price">{{ b.price }}</span>
            </h1>
            <h2 style="color: purple">{{ b.title_and_price }}</h2>
        </div>
        {% elif forloop.last %}

            <h1 style="color: purple"><a href="{{ b.get_absolute_url }}">{{ b.title }}</a> — <span class="price">{{ b.price }}</span>
            </h1>
            <h2 style="color: purple">{{ b.title_and_price }}</h2>
        </div>
        {% else %}
        <h1>№{{forloop.counter}}</h1>
        <h1>№{{forloop.revcounter}}</h1>
        <h1>{{forloop.first}}</h1>

            <h2><a href="{{ b.get_absolute_url }}">{{ b.title }}</a> — <span class="price">{{ b.price }}</span></h2>
            <h3>{{ b.title_and_price }}</h3>
        </div>
        {% endif %}
        {% endfor %}
    </div>
</div>
{% endblock %}